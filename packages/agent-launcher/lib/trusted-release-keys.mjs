// Production release signing is intentionally unprovisioned. Add an active public key here in a
// reviewed source change before enabling the protected release or update-application workflows.
// Private keys must exist only in the protected signing environment.
export const TRUSTED_RELEASE_KEYS = Object.freeze([]);
